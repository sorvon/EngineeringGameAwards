using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Threading;
public class MinSwitch : MonoBehaviour
{
    //private void OnTriggerEnter2D(Collider2D collision)
    //{
    //    if (collision.CompareTag("WaterParticle"))
    //    {
    //        if (collision.GetComponent<Rigidbody2D>().velocity.y < -1)
    //        {
    //            return;
    //        }
    //        Interlocked.Increment(ref SwitchManager.instance.minCount);
    //    }
    //}

    //private void OnTriggerExit2D(Collider2D collision)
    //{
    //    if (collision.CompareTag("WaterParticle"))
    //    {
    //        Interlocked.Decrement(ref SwitchManager.instance.minCount);
    //        if (SwitchManager.instance.minCount < 0)
    //        {
    //            Interlocked.Exchange(ref SwitchManager.instance.minCount, 0);
    //        }
    //    }
    //}
}
